apiVersion: v1
kind: Service

metadata:
  name: employee-management-service

spec:
  type: NodePort

  selector:
    app: employee-management

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080